<UserControl x:Class="StateMachineNodeEditor.ViewNodesCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StateMachineNodeEditor"
             mc:Ignorable="d" 
              Background="#FF141414" Focusable="True" AllowDrop="True" 
             local:ViewNodesCanvas.RegisterCommandBindings="{Binding CommandBindings}"
             local:ViewNodesCanvas.RegisterInputBindings="{Binding InputBindings}"
             local:ViewNodesCanvas.Add="{Binding Kek}"
             >
    <UserControl.ContextMenu>
        <ContextMenu ItemsSource="{Binding Items}">
            <!--<MenuItem Header="Delete" Name="DeleteItem" Command="Delete"/>-->
            <!--<local:ViewMenuItems Header="Add" Command="New"/>-->
        </ContextMenu>
        
    </UserControl.ContextMenu>
    <ItemsControl x:Name="grid" ItemsSource="{Binding Nodes, UpdateSourceTrigger=PropertyChanged}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <local:ViewNode />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
    <UserControl.CommandBindings>
        <CommandBinding Command="New" Executed="{Binding Add}" undo/>
    </UserControl.CommandBindings>
    <!--<UserControl.InputBindings>
        <MouseBinding Command="MediaCommands.Select" Gesture="Ctrl+LeftClick" />
    </UserControl.InputBindings>-->
</UserControl>
