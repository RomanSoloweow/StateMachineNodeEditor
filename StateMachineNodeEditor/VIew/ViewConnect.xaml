<UserControl x:Class="StateMachineNodeEditor.ViewConnect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StateMachineNodeEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <Grid>
        <Path x:Name="path" StrokeDashCap="Round" StrokeLineJoin="Round"  Stroke="{Binding Stroke, UpdateSourceTrigger=PropertyChanged}" Panel.ZIndex="800">
            <Path.Data>
                <PathGeometry  x:Name="pathGeometry" >
                    <PathFigure x:Name="pathFigure"  IsClosed="False" StartPoint="{Binding StartPoint, UpdateSourceTrigger=PropertyChanged}">
                        <BezierSegment  x:Name="bezierSegment" Point1="{Binding Point1, UpdateSourceTrigger=PropertyChanged}" Point2="{Binding Point2, UpdateSourceTrigger=PropertyChanged}" Point3="{Binding EndPoint, UpdateSourceTrigger=PropertyChanged}"/>
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
        </Path>
    </Grid>
</UserControl>
